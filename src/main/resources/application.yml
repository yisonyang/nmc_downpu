spring:
 datasource:
   driver-class-name: com.mysql.jdbc.Driver
   url: jdbc:mysql://127.0.0.1:3306/downpu
   username: "root"
   password: "yy0728"
 thymeleaf:
   prefix: classpath:/templates/
   cache: false
   encoding: UTF-8
   suffix: .html
   mode: LEGACYHTML5
 jpa:
  hibernate:
     ddl-auto: update
     show-sql: true

server:
  port: 8081
  tomcat:
    uri-encoding: UTF-8

